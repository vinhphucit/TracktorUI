<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.AppTheme" />

    <style name="Base.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="nav_text_row">
        <item name="android:textSize">@dimen/nav_row_textsize</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="nav_background_style">
        <item name="android:background">@drawable/nav_row_selector</item>
        <item name="android:clickable">true</item>
        <item name="android:paddingTop">@dimen/nav_row_padding</item>
        <item name="android:paddingLeft">@dimen/nav_row_padding_left</item>
        <item name="android:paddingBottom">@dimen/nav_row_padding</item>
    </style>

    <style name="ActionBarTitle">
        <item name="android:textSize">@dimen/action_bar_title_size</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="about_tractor_form">
        <item name="android:textSize">@dimen/normal_text_size</item>
    </style>

    <style name="invite_button">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/button_textsize</item>
        <item name="android:background">@color/purple</item>
    </style>

    <style name="list_equpment_button">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/button_textsize</item>
        <item name="android:background">@color/green</item>
    </style>
</resources>
